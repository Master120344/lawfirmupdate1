/* css/test_minimal.css */

/* === BASIC RESET === */
*, *::before, *::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    min-height: 100vh;
    position: relative; /* Essential for fixed children positioning */
    background-color: #111; /* Fallback */
    font-family: sans-serif; /* Basic font for test content */
}

/* === WALLPAPER STYLES === */
#wallpaper-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    /* --- THIS IS THE CRITICAL LINE FOR THE WALLPAPER --- */
    background-image: url('../assets/austin.jpg'); /* Path from css/ to assets/ */
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    z-index: -2; /* Behind overlay and content */
}

#wallpaper-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(20, 30, 40, 0.7); /* Adjust overlay color/opacity as needed */
    z-index: -1; /* Above wallpaper, behind content */
}

/* === HEADER STYLES (Minimal for logo placement) === */
.test-header {
    position: fixed; /* Or 'sticky' if preferred, but fixed for this test */
    top: 0;
    left: 0;
    width: 100%;
    height: 60px; /* Standard header height */
    background-color: rgba(10, 20, 30, 0.8); /* Example header background */
    padding: 0 15px; /* Horizontal padding */
    display: flex;
    align-items: center;
    justify-content: space-between; /* Pushes elements to ends */
    z-index: 10;
}

.header-text-title {
    /* This will hold "The Kershaw Law Firm P.C." text later */
    /* For now, it helps push the logo to the right using space-between */
    flex-grow: 1; /* Allow it to take up available space */
    /* Placeholder for visibility during test */
    /* content: 'Firm Title Area'; */
    /* color: white; */
    /* font-size: 10px; */
}

.test-logo-link {
    display: flex; /* Helps with vertical alignment of image if needed */
    align-items: center;
    /* No margin-left: auto needed if header-text-title flex-grows */
}

.test-logo-image {
    /* --- THIS IS CRITICAL FOR A SMALL LOGO --- */
    max-height: 35px;   /* Adjust this value to make logo smaller/larger */
    /* For example, try 30px, 35px, 40px */
    width: auto;        /* Maintain aspect ratio */
    object-fit: contain; /* Good for JPGs if they have whitespace */
    display: block;     /* Removes any extra space below inline images */
}

/* === PAGE CONTENT (Minimal for testing visibility) === */
.test-page-content {
    position: relative; /* Ensure it's above the overlay */
    z-index: 1;
    padding: 80px 20px 20px; /* Top padding for fixed header */
    color: white; /* Example text color */
}