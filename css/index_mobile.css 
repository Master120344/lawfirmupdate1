* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
    font-size: 100%; /* Base for rem units */
}

:root {
    --color-bg-main: #F4F7F6; /* Lighter, cleaner background */
    --color-bg-header-footer: rgba(255, 255, 255, 0.85); /* Brighter with slight transparency */
    --color-text-dark: #1A2E35; /* Darker, more contrasted text */
    --color-text-light: #FFFFFF;
    --color-text-medium: #4A5C62; /* For secondary text */
    --color-primary: #003355; /* A deeper, more corporate blue */
    --color-primary-darker: #002233;
    --color-accent: #D4A05D; /* A sophisticated gold/bronze accent */
    --color-accent-hover: #C0904F;
    --color-shadow: rgba(0, 22, 33, 0.1); /* Softer shadow */
    --color-border: rgba(0, 34, 51, 0.15);

    --font-primary: 'Cinzel', serif; /* For headings, logo */
    --font-secondary: 'Open Sans', sans-serif; /* For body text, UI elements */

    --header-height: 70px;
    --footer-height: 65px; /* Slightly taller for icons + text */
    --border-radius-main: 8px;
    --border-radius-small: 4px;
    --container-padding: 0 15px;
}

body {
    font-family: var(--font-secondary);
    min-height: 100vh;
    color: var(--color-text-dark);
    line-height: 1.7;
    background-color: var(--color-bg-main);
    overflow-x: hidden;
    position: relative;
    opacity: 0;
    transition: opacity 0.7s ease-in-out;
    padding-top: var(--header-height); /* For fixed header */
    padding-bottom: var(--footer-height); /* Adjust padding for fixed tabs */
    display: flex;
    flex-direction: column;
    font-size: 1rem; /* Approx 16px */
}

body.loaded {
    opacity: 1;
}

#background-static {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -2;
    background-size: cover;
    background-position: center 30%; /* Adjust focus of background */
    background-repeat: no-repeat;
    background-image: url('../assets/austinbackground.jpg');
    animation: subtleZoom 60s infinite alternate ease-in-out;
}

.background-overlay { /* Added to improve text legibility over background */
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background: linear-gradient(to bottom, rgba(0, 34, 51, 0.4), rgba(0, 34, 51, 0.7));
    opacity: 0.8;
}


@keyframes subtleZoom {
    from { transform: scale(1); }
    to { transform: scale(1.03); }
}

.loader {
    position: fixed; top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    width: 45px; height: 45px;
    border: 5px solid rgba(212, 160, 93, 0.3); /* Accent color for loader */
    border-top: 5px solid var(--color-accent);
    border-radius: 50%;
    animation: spin 1s linear infinite;
    z-index: 3000; opacity: 1;
    transition: opacity 0.5s ease-out;
    pointer-events: auto;
}
.loader.hidden { opacity: 0; pointer-events: none; }
@keyframes spin {
    0% { transform: translate(-50%, -50%) rotate(0deg); }
    100% { transform: translate(-50%, -50%) rotate(360deg); }
}

/* --- HEADER --- */
.header {
    position: fixed; /* Make header fixed */
    top: 0; left: 0; width: 100%;
    z-index: 1000;
    height: var(--header-height);
    background: var(--color-bg-header-footer);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    box-shadow: 0 2px 10px var(--color-shadow);
    color: var(--color-primary);
    border-bottom: 1px solid var(--color-border);
    display: flex;
    align-items: center;
}

.header-content {
    width: 90%; margin: 0 auto;
    max-width: 1200px; /* For potential desktop consistency */
    display: flex;
    justify-content: center; /* Center logo as no other elements */
    align-items: center;
}

.logo-container {
    text-align: center;
}

.logo-link { text-decoration: none; color: inherit; }

.logo-container h1 {
    font-family: var(--font-primary);
    font-size: 1.5rem; /* Adjusted */
    font-weight: 600;
    margin: 0;
    color: var(--color-primary);
    letter-spacing: 0.5px;
    transition: color 0.3s ease;
}
.logo-container .logo-tagline {
    font-family: var(--font-secondary);
    font-size: 0.7rem;
    color: var(--color-text-medium);
    text-transform: uppercase;
    letter-spacing: 1px;
    display: block;
    margin-top: 2px;
}

.logo-link:hover h1 { color: var(--color-accent); }


/* --- MAIN CONTENT --- */
.main-content-area {
    flex-grow: 1; display: flex;
    flex-direction: column; /* Stack sections */
    align-items: center;
    justify-content: flex-start; /* Content starts from top */
    padding: 30px var(--container-padding);
    position: relative;
    z-index: 1;
}

.main-content-container {
    width: 100%;
    max-width: 600px; /* Control content width for readability */
    display: flex;
    flex-direction: column;
    gap: 40px; /* Space between sections */
}

/* --- HERO SECTION --- */
.hero {
    text-align: center;
    padding: 20px 0;
    color: var(--color-text-light); /* Text on dark background */
}

.hero-text-content {
    background-color: rgba(0, 22, 33, 0.3); /* Subtle dark card for hero text */
    padding: 25px 20px;
    border-radius: var(--border-radius-main);
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    margin-bottom: 30px;
    animation: fadeInUp 0.8s ease-out 0.2s backwards;
}

.hero-subtitle {
    font-family: var(--font-secondary);
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 1.5px;
    margin-bottom: 8px;
}

.hero-title {
    font-family: var(--font-primary);
    font-size: clamp(2rem, 7vw, 2.8rem); /* Responsive font size */
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 15px;
    text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
}

.hero-description {
    font-size: 1.05rem;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.9);
    max-width: 450px;
    margin: 0 auto;
    font-weight: 400;
}

.cta-button {
    display: inline-flex; align-items: center; gap: 12px;
    padding: 14px 32px;
    background: var(--color-accent);
    color: var(--color-primary-darker); /* Dark text on gold for contrast */
    text-decoration: none;
    font-family: var(--font-primary); font-size: 1.1rem; font-weight: 700;
    border-radius: 50px; border: none;
    transition: all 0.3s ease;
    box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    cursor: pointer;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    animation: fadeInUp 0.8s ease-out 0.5s backwards;
}
.cta-button i { font-size: 1em; }
.cta-button:hover, .cta-button:focus {
    background: var(--color-accent-hover);
    color: var(--color-primary);
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 8px 20px rgba(0,0,0,0.25);
}

/* USP (Unique Selling Propositions) Section */
.usp-section {
    display: flex;
    flex-direction: column;
    gap: 25px;
    padding: 20px 0;
    animation: fadeInUp 0.8s ease-out 0.7s backwards;
}

.usp-item {
    background-color: rgba(255, 255, 255, 0.05); /* Very subtle card on dark overlay */
    padding: 20px;
    border-radius: var(--border-radius-main);
    text-align: center;
    color: var(--color-text-light);
    border: 1px solid rgba(255,255,255,0.1);
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.usp-icon {
    font-size: 2rem;
    color: var(--color-accent);
    margin-bottom: 15px;
}

.usp-item h4 {
    font-family: var(--font-primary);
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--color-text-light);
    margin-bottom: 8px;
}

.usp-item p {
    font-size: 0.95rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.85);
}


/* --- TABS (BOTTOM NAVIGATION) --- */
.tabs {
    position: fixed; bottom: 0; left: 0; width: 100%;
    z-index: 1000;
    height: var(--footer-height);
    background: var(--color-bg-header-footer); /* Match header */
    backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);
    box-shadow: 0 -2px 10px var(--color-shadow);
    border-top: 1px solid var(--color-border);
    display: flex;
    justify-content: space-around;
    align-items: stretch; /* Make items fill height */
    color: var(--color-text-dark);
    padding: 0;
}
.tabs a {
    color: var(--color-text-medium);
    text-decoration: none;
    font-family: var(--font-secondary);
    font-size: 0.65rem; /* Smaller text for icon + text */
    font-weight: 600;
    transition: all 0.25s ease;
    text-transform: uppercase;
    letter-spacing: 0.3px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    flex-grow: 1;
    padding: 5px 3px; /* Adjust padding */
    position: relative; /* For potential active indicator */
    line-height: 1.3;
}
.tabs a i {
    font-size: 1.3rem; /* Larger icons */
    margin-bottom: 4px;
    transition: transform 0.2s ease-out;
}
.tabs a:hover {
    color: var(--color-primary);
}
.tabs a:hover i {
    transform: scale(1.1);
}

.tabs a.active {
    color: var(--color-primary); /* Primary color for active tab */
    font-weight: 700;
}
.tabs a.active::before { /* Top border indicator */
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background-color: var(--color-accent);
    border-radius: 0 0 2px 2px;
}
.tabs a.active i {
    color: var(--color-primary); /* Can also accent active icon */
}


/* Animation */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Accessibility Focus */
a:focus-visible, button:focus-visible, input:focus-visible, [tabindex]:not([tabindex="-1"]):focus-visible {
    outline: 3px solid var(--color-accent);
    outline-offset: 2px;
    border-radius: var(--border-radius-small);
    box-shadow: 0 0 0 3px var(--color-primary-darker); /* Fallback or extra emphasis */
}

/* Remove default outline for elements that have focus-visible */
a:focus, button:focus, input:focus, [tabindex]:not([tabindex="-1"]):focus {
    outline: none;
}