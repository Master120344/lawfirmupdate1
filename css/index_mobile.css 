* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

:root {
    --color-bg-header-footer: rgba(255, 255, 255, 0.95);
    --color-text-header-footer: #1a2a44;
    --color-text-on-dark-bg: #ffffff;
    --color-accent-blue-primary: #003087;
    --color-accent-blue-secondary: #3b82f6;
    --color-shadow-text: 1px 1px 4px rgba(0, 0, 0, 0.7);
    --color-shadow-subtle: rgba(0, 0, 0, 0.1);
    --color-border-subtle: rgba(0, 0, 0, 0.1);
    --font-primary: 'Cinzel', serif;
    --font-secondary: 'EB Garamond', serif;
    --footer-height: 60px;
}

body {
    font-family: var(--font-secondary);
    min-height: 100vh;
    color: var(--color-text-on-dark-bg);
    line-height: 1.6;
    background-color: #f8fafc;
    overflow-x: hidden;
    position: relative;
    opacity: 0;
    transition: opacity 0.7s ease-in-out;
    padding-bottom: var(--footer-height);
    display: flex;
    flex-direction: column;
    font-size: 16px;
}

body.loaded {
    opacity: 1;
}

#background-static {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -2;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    background-image: url('../assets/austin.jpg');
    animation: subtleZoom 30s infinite alternate ease-in-out;
}

@keyframes subtleZoom {
    from { transform: scale(1); }
    to { transform: scale(1.05); }
}

.loader {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 40px;
    height: 40px;
    border: 4px solid rgba(59, 130, 246, 0.3);
    border-top: 4px solid var(--color-accent-blue-primary);
    border-radius: 50%;
    animation: spin 1s linear infinite;
    z-index: 3000;
    opacity: 1;
    transition: opacity 0.5s ease-out;
    pointer-events: auto;
}

.loader.hidden {
    opacity: 0;
    pointer-events: none;
}

@keyframes spin {
    0% { transform: translate(-50%, -50%) rotate(0deg); }
    100% { transform: translate(-50%, -50%) rotate(360deg); }
}

.header {
    position: sticky;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 20;
    padding: 10px 0;
    background: var(--color-bg-header-footer);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    box-shadow: 0 2px 4px var(--color-shadow-subtle);
    color: var(--color-text-header-footer);
}

.header-content {
    width: 90%;
    margin: 0 auto;
    display: flex;
    justify-content: center;
    align-items: center;
}

.logo-container {
    text-align: center;
}

.logo-image {
    height: 40px;
    width: auto;
    transition: transform 0.3s ease;
}

.logo-link:hover .logo-image {
    transform: scale(1.05);
}

.main-content-area {
    position: relative;
    z-index: 1;
    flex-grow: 1;
    padding: 20px 0;
}

.main-content-container {
    width: 90%;
    max-width: 600px;
    margin: 0 auto;
}

.hero {
    position: relative;
    text-align: center;
    padding: 40px 20px;
    border-radius: 15px;
    overflow: hidden;
    margin-bottom: 30px;
}

.hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 48, 135, 0.6);
    z-index: -1;
}

.hero h1 {
    font-family: var(--font-primary);
    font-size: 2.2rem;
    color: var(--color-text-on-dark-bg);
    font-weight: 700;
    text-shadow: var(--color-shadow-text);
    margin-bottom: 15px;
    animation: fadeIn 1s ease-out;
}

.hero p {
    font-family: var(--font-secondary);
    font-size: 1.1rem;
    color: var(--color-text-on-dark-bg);
    margin-bottom: 20px;
    animation: fadeIn 1s ease-out 0.2s backwards;
}

.cta-button {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 30px;
    background: var(--color-accent-blue-primary);
    color: var(--color-text-on-dark-bg);
    text-decoration: none;
    font-family: var(--font-primary);
    font-size: 1rem;
    font-weight: 700;
    border-radius: 50px;
    border: none;
    transition: all 0.3s ease;
    box-shadow: 0 4px 12px rgba(0, 48, 135, 0.3);
    cursor: pointer;
    animation: fadeIn 1s ease-out 0.4s backwards;
}

.cta-button i {
    font-size: 0.9em;
}

.cta-button:hover {
    background: var(--color-accent-blue-secondary);
    transform: translateY(-2px);
    box-shadow: 0 6px 15px rgba(59, 130, 246, 0.4);
}

.cta-button.secondary {
    background: transparent;
    border: 2px solid var(--color-text-on-dark-bg);
}

.cta-button.secondary:hover {
    background: var(--color-accent-blue-secondary);
    border-color: var(--color-accent-blue-secondary);
}

.services {
    margin-bottom: 40px;
}

.services h2 {
    font-family: var(--font-primary);
    font-size: 1.8rem;
    color: var(--color-text-header-footer);
    text-align: center;
    margin-bottom: 20px;
}

.service-card {
    background: var(--color-bg-header-footer);
    border-radius: 10px;
    padding: 20px;
    margin-bottom: 15px;
    text-align: center;
    box-shadow: 0 2px 8px var(--color-shadow-subtle);
    transition: transform 0.3s ease;
}

.service-card:hover {
    transform: translateY(-5px);
}

.service-card i {
    font-size: 2rem;
    color: var(--color-accent-blue-primary);
    margin-bottom: 10px;
}

.service-card h3 {
    font-family: var(--font-primary);
    font-size: 1.3rem;
    color: var(--color-text-header-footer);
    margin-bottom: 10px;
}

.service-card p {
    font-family: var(--font-secondary);
    font-size: 1rem;
    color: #4b5563;
}

.testimonials {
    margin-bottom: 40px;
}

.testimonials h2 {
    font-family: var(--font-primary);
    font-size: 1.8rem;
    color: var(--color-text-header-footer);
    text-align: center;
    margin-bottom: 20px;
}

.testimonial-container {
    display: flex;
    overflow-x: auto;
    gap: 15px;
    padding-bottom: 10px;
    scroll-snap-type: x mandatory;
}

.testimonial-card {
    flex: 0 0 80%;
    background: var(--color-bg-header-footer);
    border-radius: 10px;
    padding: 20px;
    box-shadow: 0 2px 8px var(--color-shadow-subtle);
    scroll-snap-align: start;
}

.testimonial-card .stars {
    color: #f59e0b;
    margin-bottom: 10px;
}

.testimonial-card p {
    font-family: var(--font-secondary);
    font-size: 1rem;
    color: #4b5563;
    margin-bottom: 10px;
}

.testimonial-card span {
    font-family: var(--font-primary);
    font-size: 0.9rem;
    color: var(--color-accent-blue-primary);
}

.contact-cta {
    text-align: center;
    padding: 30px 20px;
    background: var(--color-accent-blue-primary);
    border-radius: 15px;
    margin-bottom: 30px;
}

.contact-cta h2 {
    font-family: var(--font-primary);
    font-size: 1.8rem;
    color: var(--color-text-on-dark-bg);
    margin-bottom: 10px;
}

.contact-cta p {
    font-family: var(--font-secondary);
    font-size: 1rem;
    color: var(--color-text-on-dark-bg);
    margin-bottom: 20px;
}

.tabs {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
    height: var(--footer-height);
    background: var(--color-bg-header-footer);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    box-shadow: 0 -2px 4px var(--color-shadow-subtle);
    border-top: 1px solid var(--color-border-subtle);
    display: flex;
    justify-content: space-around;
    align-items: center;
}

.tabs a {
    color: var(--color-text-header-footer);
    text-decoration: none;
    font-family: var(--font-secondary);
    font-size: 0.75rem;
    font-weight: 500;
    padding: 8px 5px;
    transition: all 0.3s ease;
    text-align: center;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
}

.tabs a i {
    font-size: 1rem;
}

.tabs a.active {
    color: var(--color-accent-blue-primary);
    font-weight: 700;
}

.tabs a:hover {
    color: var(--color-accent-blue-primary);
}

@keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}

a:focus-visible, button:focus-visible {
    outline: 2px solid var(--color-accent-blue-secondary);
    outline-offset: 2px;
    border-radius: 3px;
}