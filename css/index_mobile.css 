/* Basic Reset and Box Sizing */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
    font-size: 16px;
    background-color: #e0f2f7; /* Light blue fallback */
}

body {
    font-family: 'Roboto', sans-serif; /* A modern, readable sans-serif font */
    min-height: 100vh;
    color: #333;
    line-height: 1.6;
    background-color: transparent; /* For the background image */
    overflow-x: hidden;
    position: relative;
    opacity: 0;
    transition: opacity 0.5s ease-in-out;
    padding-top: 60px; /* Adjust for fixed header */
    padding-bottom: 55px; /* Adjust for fixed tabs */
    display: flex;
    flex-direction: column;
}

body.loaded {
    opacity: 1;
}

/* Full-screen, fixed background image */
#background-static {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -2;
    background-image: url('../assets/austinbackground.jpg'); /* Ensure this path is correct! */
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
}

/* Subtle overlay for better text readability on the background */
body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(255, 255, 255, 0.7); /* Semi-transparent white */
    z-index: -1;
}

/* Loader */
.loader {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 50px;
    height: 50px;
    border: 5px solid #bbdefb;
    border-top: 5px solid #29b6f6;
    border-radius: 50%;
    animation: spin 1.2s linear infinite;
    z-index: 3000;
    opacity: 1;
    transition: opacity 0.3s ease-out;
    pointer-events: auto;
}

.loader.hidden {
    opacity: 0;
    pointer-events: none;
}

@keyframes spin {
    0% { transform: translate(-50%, -50%) rotate(0deg); }
    100% { transform: translate(-50%, -50%) rotate(360deg); }
}

/* Fixed Header */
.header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    background: #fff;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    padding: 10px 15px;
    z-index: 100;
}

.header-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: 600px;
    margin: 0 auto;
}

.logo-link {
    display: flex;
    align-items: center;
    text-decoration: none;
    color: #333;
}

.logo {
    height: 40px; /* Fixed, smaller logo height */
    margin-right: 10px;
}

.header-content h1 {
    font-family: 'Montserrat', sans-serif; /* A clean, modern font for the title */
    font-size: 1.5rem;
    font-weight: 600;
    margin: 0;
}

/* Main Content Area */
.main-content-area {
    flex-grow: 1;
    padding: 20px;
    padding-top: 70px; /* Adjust for header */
    background: transparent;
}

.main-content-container {
    max-width: 600px;
    margin: 0 auto;
}

/* Hero Section */
.hero {
    background: #fff;
    padding: 30px 20px;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    margin-bottom: 20px;
    text-align: center;
}

.hero h2 {
    font-family: 'Playfair Display', serif;
    font-size: 2.2rem;
    color: #29b6f6;
    margin-bottom: 15px;
}

.hero p {
    color: #555;
    margin-bottom: 20px;
}

.cta-button {
    display: inline-block;
    padding: 12px 25px;
    background-color: #4caf50; /* A vibrant green */
    color: #fff;
    text-decoration: none;
    border-radius: 5px;
    font-weight: 500;
    transition: background-color 0.3s ease;
}

.cta-button:hover {
    background-color: #388e3c;
}

/* Featured Services */
.featured-services {
    background: #fff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    margin-bottom: 20px;
}

.featured-services h3 {
    font-family: 'Montserrat', sans-serif;
    font-size: 1.6rem;
    color: #29b6f6;
    margin-bottom: 15px;
    text-align: center;
}

.services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 15px;
}

.service-card {
    padding: 15px;
    background-color: #f9f9f9;
    border-radius: 5px;
    text-align: center;
}

.service-card i {
    font-size: 2rem;
    color: #29b6f6;
    margin-bottom: 10px;
}

.service-card h4 {
    font-size: 1.1rem;
    color: #333;
    margin-bottom: 10px;
}

.service-card p {
    font-size: 0.9rem;
    color: #555;
}

/* Tabs Navigation */
.tabs {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    background: #fff;
    box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.1);
    display: flex;
    justify-content: space-around;
    align-items: center;
    height: 55px;
    z-index: 100;
}

.tabs a {
    color: #555;
    text-decoration: none;
    flex-grow: 1;
    text-align: center;
    padding: 8px 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

.tabs a i {
    font-size: 1.2rem;
    margin-bottom: 5px;
}

.tabs a.active {
    color: #29b6f6;
    font-weight: 500;
}

.tabs a:hover {
    color: #29b6f6;
}

/* Footer */
.footer {
    background: #333;
    color: #f4f4f4;
    text-align: center;
    padding: 15px 0;
    font-size: 0.8rem;
}

.footer p {
    margin: 5px 0;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 480px) {
    .header-content h1 {
        font-size: 1.3rem;
    }
    .logo {
        height: 35px;
    }
    .hero h2 {
        font-size: 2rem;
    }
    .services-grid {
        grid-template-columns: 1fr; /* Stack services on smaller screens */
    }
}

/* Import Roboto and Montserrat fonts from Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&family=Montserrat:wght@400;600&family=Playfair+Display:wght@400;700&display=swap');
